import * as React from 'react';
import {render} from '@testing-library/react-native';
import {AnimationScreen} from '../AnimationScreen';

describe('AnimationScreen', () => {
  it('renders', () => {
    render(<AnimationScreen />);
  });
});
